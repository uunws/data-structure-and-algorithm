#include <iostream>
#include <vector>
using namespace std;

int main() {
    int w;
    int n;
    cin >> w >> n;
    vector<int> y;
    for (int i = 0; i < n; i++) {
        cin >> y[i];
    }
    // output : ความเลิศ (จนเตม1ตัว)
}